[defaults]
inventory = inventory.yml
pipelining = True
# host_key_checking = False  # INSECURE: only enable for initial provisioning
#                             # of hosts whose keys you cannot pre-verify.
#                             # Disabling this silently accepts unknown host keys
#                             # and opens the connection to MITM attacks.
retry_files_enabled = False
